import React from "react";

const BlogFormSkeleton = () => {
    return (
        <main className="p-2 relative bg-gray-200 h-[100vh] w-full flex items-center justify-center">
            <p className="text-2xl text-gray-700 font-semibold">Loading ...</p>
        </main>
    );
};

export default BlogFormSkeleton;
